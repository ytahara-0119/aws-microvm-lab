# --------------------------------------------------------------------
# STEP18 ParrotOS Desktop（サブプロジェクト）
#
# STEP1〜17のAI Desktop Agentの流れとは別枠。
# 「本物のParrotOSをGUIで人間が直接操作できるようにする」ことが目的。
#
# common.envのVNC_PORT(6080)/TARGET_PORT(6080)/URL(/vnc.html)を
# そのまま利用する。TERMINAL_PORT(7681)はこのSTEPでは未使用だが、
# 害はないため上書きしていない。
# --------------------------------------------------------------------

STEP_DIR=step18-parrotos-desktop

IMAGE_VERSION=7.0
